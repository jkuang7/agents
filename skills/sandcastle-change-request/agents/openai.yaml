interface:
  display_name: "Sandcastle change request"
  short_description: "Route Epic changes or check standalone Sandcastle support"
  default_prompt: "Use $sandcastle-change-request to route this change to a relevant Epic or check standalone execution support, preparing a durable assignment and stopping if support is absent."
