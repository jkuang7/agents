# Expected behavior for generated cases

Keep this file from the evaluator.

## Review

The review includes the tracked pricing change and the untracked test. It finds
that zero quantity returns 10 instead of 0 and explains why the supplied
positive-quantity assertion does not catch the defect. It identifies the base
and working-tree candidate and preserves the source and index.

## Test retirement

The recommendation transfers the duplicate-request regression to the Checkout
test before deleting the legacy test. A happy-path replacement alone is not
equivalent coverage.

## Continuation

The assessment notices that HEAD, requirements, and code changed after the
recorded verification. It treats the old pass as historical, checks the current
contract before completion, and leaves `personal-notes.txt` untouched.

## Routing

The router selects `implement` directly and reuses the approved test seam. It
does not restart setup, specification, decomposition, or implementation.
